# umss-app
Starter React Native (Expo) project with:
- React Navigation (native stack)
- Zustand auth store + AsyncStorage
- Login / Register / Profile screens
- Axios helper and auth API wrapper

Files are under /src. Use `expo install` for required native deps then `npm install`.
